/**
 * Tipos genéricos para respuestas de API
 * Re-exportados desde api/types.ts para acceso centralizado
 */
export type { ApiResponse, PaginatedResponse, ErrorResponse, QueryParams } from '@/api/types';