/**
 * @deprecated Este archivo es código muerto del template de Vite.
 * La aplicación real se renderiza desde main.tsx que define su propio componente App.
 * Mantenido temporalmente para referencia, será eliminado en la próxima limpieza.
 */
export {};